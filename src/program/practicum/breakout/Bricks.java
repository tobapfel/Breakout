package program.practicum.breakout;

import acm.graphics.GRect;

public class Bricks extends GRect {

	public Bricks(int row, int col) {
		super(col * 10 + row * 2, 50 + row * 5 + (row - 1) * 2);
		
		switch(col){
		case 1:
		case 2:
			GRect.
		case 3:
		case 4:
		case 5:
		case 6:
		case 7:
		case 8:
		}
	}

}
